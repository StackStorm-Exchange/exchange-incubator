---
description: 
pack: aws_apigateway
enabled: true
entry_point: run.py
name: put_method
runner_type: python-script
parameters:
  action:
    default: put_method
    immutable: true
    type: string
  cls:
    default: apigateway
    type: string
  headers:
    type: string
  module_path:
    default: boto3
    immutable: true
    type: string
  restApiId:
    type: string
    description: "The RestApi identifier for the new Method resource."
    required: true
  resourceId:
    type: string
    description: "The Resource identifier for the new Method resource."
    required: true
  httpMethod:
    type: string
    description: "Specifies the put method request's HTTP method type."
    required: true
  authorizationType:
    type: string
    description: "Specifies the type of authorization used for the method."
    required: true
  authorizerId:
    type: string
    description: "Specifies the identifier of an Authorizer to use on this Method, if the type is CUSTOM."
  apiKeyRequired:
    type: boolean
    description: "Specifies whether the method required a valid ApiKey."
  requestParameters:
    type: object
    description: "Represents requests parameters that are sent with the backend request. Request parameters are represented as a key/value map, with a destination as the key and a source as the value. A source must match an existing method request parameter, or a static value. Static values must be enclosed with single quotes, and be pre-encoded based on their destination in the request. The destination must match the pattern integration.request.{location}.{name}, where location is either querystring, path, or header. name must be a valid, unique parameter name."
  requestModels:
    type: object
    description: "Specifies the Model resources used for the request's content type. Request models are represented as a key/value map, with a content type as the key and a Model name as the value."