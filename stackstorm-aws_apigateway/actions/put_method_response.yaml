---
description: 
pack: aws_apigateway
enabled: true
entry_point: run.py
name: put_method_response
runner_type: python-script
parameters:
  action:
    default: put_method_response
    immutable: true
    type: string
  cls:
    default: apigateway
    type: string
  headers:
    type: string
  module_path:
    default: boto3
    immutable: true
    type: string
  restApiId:
    type: string
    description: "The RestApi identifier for the Method resource."
    required: true
  resourceId:
    type: string
    description: "The Resource identifier for the Method resource."
    required: true
  httpMethod:
    type: string
    description: "The HTTP verb that identifies the Method resource."
    required: true
  statusCode:
    type: string
    description: "The method response's status code."
    required: true
  responseParameters:
    type: object
    description: "Represents response parameters that can be sent back to the caller by Amazon API Gateway. Response parameters are represented as a key/value map, with a destination as the key and a Boolean flag as the value. The Boolean flag is used to specify whether the parameter is required. A destination must match the pattern method.response.header.{name}, where name is a valid, unique header name. Destinations specified here are available to the integration for mapping from integration response parameters."
  responseModels:
    type: object
    description: "Specifies the Model resources used for the response's content type. Response models are represented as a key/value map, with a content type as the key and a Model name as the value."