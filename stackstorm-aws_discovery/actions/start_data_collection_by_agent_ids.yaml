---
description: 
pack: aws_discovery
enabled: true
entry_point: run.py
name: start_data_collection_by_agent_ids
runner_type: python-script
parameters:
  action:
    default: start_data_collection_by_agent_ids
    immutable: true
    type: string
  cls:
    default: discovery
    type: string
  headers:
    type: string
  module_path:
    default: boto3
    immutable: true
    type: string
  agentIds:
    type: array
    description: "The IDs of the agents that you want to start collecting data. If you send a request to an AWS agent ID that you do not have permission to contact, according to your AWS account, the service does not throw an exception. Instead, it returns the error in the Description field. If you send a request to multiple agents and you do not have permission to contact some of those agents, the system does not throw an exception. Instead, the system shows Failed in the Description field."
    required: true