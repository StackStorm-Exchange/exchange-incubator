---
description: 
pack: aws_elasticbeanstalk
enabled: true
entry_point: run.py
name: describe_instances_health
runner_type: python-script
parameters:
  action:
    default: describe_instances_health
    immutable: true
    type: string
  cls:
    default: elasticbeanstalk
    type: string
  headers:
    type: string
  module_path:
    default: boto3
    immutable: true
    type: string
  EnvironmentName:
    type: string
    description: "Specifies the AWS Elastic Beanstalk environment name."
  EnvironmentId:
    type: string
    description: "Specifies the AWS Elastic Beanstalk environment ID."
  AttributeNames:
    type: array
    description: "Specifies the response elements you wish to receive. If no attribute names are specified, AWS Elastic Beanstalk only returns a list of instances."
  NextToken:
    type: string
    description: "Specifies the next token of the request."