---
description: 
pack: aws_iam
enabled: true
entry_point: run.py
name: get_group_policy
runner_type: python-script
parameters:
  action:
    default: get_group_policy
    immutable: true
    type: string
  cls:
    default: iam
    type: string
  headers:
    type: string
  module_path:
    default: boto3
    immutable: true
    type: string
  GroupName:
    type: string
    description: "The name of the group the policy is associated with. The regex pattern for this parameter is a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: =,.@-"
    required: true
  PolicyName:
    type: string
    description: "The name of the policy document to get. The regex pattern for this parameter is a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: =,.@-"
    required: true