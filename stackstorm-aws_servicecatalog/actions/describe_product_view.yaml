---
description: 
pack: aws_servicecatalog
enabled: true
entry_point: run.py
name: describe_product_view
runner_type: python-script
parameters:
  action:
    default: describe_product_view
    immutable: true
    type: string
  cls:
    default: servicecatalog
    type: string
  headers:
    type: string
  module_path:
    default: boto3
    immutable: true
    type: string
  Id:
    type: string
    description: "The ProductViewId of the product to describe."
    required: true
  AcceptLanguage:
    type: string
    description: "Optional language code. Supported language codes are as follows: 'en' (English) 'jp' (Japanese) 'zh' (Chinese) If no code is specified, 'en' is used as the default."